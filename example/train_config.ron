(
    min_learning_rate: 1e-5,
    max_learning_rate: 2e-4,
    training_iterations: 60000,
    warmup_iterations: 3000,
    evaluation_interval: 500,
    evaluation_iterations: 5,
    contexts_per_batch: 64
)